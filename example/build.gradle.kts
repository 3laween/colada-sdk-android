// Root build file for the example app. Empty on purpose — all real configuration lives in
// app/build.gradle.kts. This file exists because the Android Gradle plugin expects a root
// project even when it configures nothing itself.
